var Foto = function(instructionStatus,reservationStation,registerFile,clock){
		this.instructionStatus = instructionStatus;
		this.reservationStation = reservationStation;
		this.registerFile = registerFile;
		this.clock = clock;
};